### 1. Android 四大组件

四大组件是应用的基本构成单元：Activity、Service、BroadcastReceiver、ContentProvider。它们都需在清单文件中注册，由AMS（Activity Manager Service）统一调度。

**原理简述**  
- **Activity**：负责界面展示，生命周期由系统管理。启动时通过Instrumentation和AMS交互，最终创建Activity实例并回调onCreate。  
- **Service**：后台执行任务，有started和bound两种模式。started通过startService启动，运行在主线程，需自行创建子线程；bound Service通过bindService获取Binder对象进行跨进程通信。  
- **BroadcastReceiver**：用于接收系统或应用广播，分为静态注册（清单声明）和动态注册（代码注册）。发送广播通过sendBroadcast（普通）或sendOrderedBroadcast（有序）。Android 8.0后限制隐式广播静态注册。  
- **ContentProvider**：统一管理应用数据共享，基于URI匹配，底层使用Binder实现跨进程CRUD操作。  

**示例代码**  
```kotlin
// Activity
class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
    }
}

// Service
class MyService : Service() {
    override fun onBind(intent: Intent?) = null
    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
        thread { /* 后台任务 */ }
        return START_STICKY
    }
}

// BroadcastReceiver 动态注册
val receiver = object : BroadcastReceiver() {
    override fun onReceive(context: Context?, intent: Intent?) { }
}
val filter = IntentFilter("com.example.MY_ACTION")
registerReceiver(receiver, filter)

// ContentProvider
class MyProvider : ContentProvider() {
    override fun query(uri: Uri, projection: Array<out String>?, selection: String?, selectionArgs: Array<out String>?, sortOrder: String?): Cursor? {
        // 根据uri匹配表
    }
    // 其他方法...
}
```

---

### 2. 自定义 View

**原理与流程**  
- **View绘制流程**：measure（确定大小）→ layout（确定位置）→ draw（绘制）。  
- **关键方法**：`onMeasure(widthMeasureSpec, heightMeasureSpec)`，需处理MeasureSpec模式（EXACTLY、AT_MOST、UNSPECIFIED），调用setMeasuredDimension。`onDraw(Canvas)`中通过Paint、Path等完成绘制。  
- **自定义属性**：在res/values/attrs.xml中声明，构造方法解析TypedArray。  
- **事件处理**：结合触摸事件重写onTouchEvent实现交互。  
- **性能优化**：避免在onDraw中创建对象，使用clipRect等减少绘制区域。

**示例（圆形进度条）**  
```kotlin
class CircleProgressView @JvmOverloads constructor(
    context: Context, attrs: AttributeSet? = null, defStyleAttr: Int = 0
) : View(context, attrs, defStyleAttr) {

    private var progress = 0f
    private val paint = Paint(Paint.ANTI_ALIAS_FLAG).apply {
        color = Color.BLUE; style = Paint.Style.STROKE; strokeWidth = 10f
    }

    fun setProgress(p: Float) {
        progress = p
        invalidate()
    }

    override fun onDraw(canvas: Canvas) {
        super.onDraw(canvas)
        val centerX = width / 2f; val centerY = height / 2f
        val radius = (Math.min(width, height) / 2f) - paint.strokeWidth
        canvas.drawCircle(centerX, centerY, radius, paint.apply {
            color = Color.GRAY; strokeWidth = 5f; style = Paint.Style.STROKE
        })
        paint.apply { color = Color.BLUE; strokeWidth = 10f; style = Paint.Style.STROKE }
        val sweepAngle = 360 * progress / 100
        canvas.drawArc(centerX - radius, centerY - radius, centerX + radius, centerY + radius,
            -90f, sweepAngle, false, paint)
    }
}
```

---

### 3. 事件分发

**核心机制**  
事件序列从Activity→PhoneWindow→DecorView→ViewGroup→目标View。三个关键方法：  
- `dispatchTouchEvent`：事件分发入口。  
- `onInterceptTouchEvent`（ViewGroup）：是否拦截事件，默认返回false。  
- `onTouchEvent`：处理事件。  
事件传递遵循“自顶向下分发，自底向上处理”的U型模型。一旦一个View决定消费（onTouchEvent返回true），同一事件序列后续事件直接交给它。ViewGroup可通过requestDisallowInterceptTouchEvent要求父View不拦截。

**伪代码流程**  
```java
// ViewGroup伪代码
public boolean dispatchTouchEvent(MotionEvent ev) {
    boolean handled = false;
    if (onInterceptTouchEvent(ev)) {
        handled = onTouchEvent(ev);
    } else {
        for (child in children) {
            if (child.dispatchTouchEvent(ev)) {
                handled = true;
                break;
            }
        }
        if (!handled) handled = onTouchEvent(ev);
    }
    return handled;
}
```
**解决滑动冲突**常见方案：外部拦截法（父View判断何时拦截）和内部拦截法（子View调用requestDisallow…父View在onIntercept中配合）。

---

### 4. 线程通信

Android中主线程不能执行耗时任务，子线程不能更新UI。常用方案：

| 方案 | 原理 | 适用场景 |
|------|------|----------|
| **Handler+MessageQueue+Looper** | Handler将Message发送到Looper管理的MessageQueue，Looper循环取出分发给Handler.handleMessage。主线程默认Looper已启动，子线程需手动prepare和loop。 | 经典线程切换 |
| **AsyncTask** (已废弃) | 内部封装线程池和Handler | 简单异步任务 |
| **RxJava** | 观察者模式，支持线程调度（Schedulers.io(), AndroidSchedulers.mainThread()） | 复杂异步流 |
| **Kotlin Coroutines** | 挂起函数、结构化并发。通过Dispatchers切换线程（Main/IO/Default） | 现代推荐方案 |
| **LiveData / Flow** | 生命周期感知的数据持有者，setValue在主线程，postValue内部切换 | UI数据绑定 |

**Coroutine 示例**  
```kotlin
class MyViewModel : ViewModel() {
    fun loadData() {
        viewModelScope.launch(Dispatchers.IO) {
            val data = api.fetch()     // 子线程网络请求
            withContext(Dispatchers.Main) {
                textView.text = data   // 主线程更新UI
            }
        }
    }
}
```

---

### 5. 数据存储

**五种存储方式原理及代码**  

1. **SharedPreferences**：基于XML文件，键值对。初次加载文件到内存，commit同步写磁盘，apply异步。不适合大量数据。  
   ```kotlin
   val sp = getSharedPreferences("my_pref", Context.MODE_PRIVATE)
   sp.edit().putString("key", "value").apply()
   ```

2. **Internal/External Storage**：内部存储私密，外部存储需要权限。  
   ```kotlin
   val file = File(filesDir, "myfile.txt")
   file.writeText("Hello")
   ```

3. **SQLite / Room**：Room是SQLite的抽象层，编译时校验SQL，提供LiveData/Flow支持。  
   ```kotlin
   @Entity data class User(@PrimaryKey val id: Int, val name: String)
   @Dao interface UserDao {
       @Query("SELECT * FROM user") fun getAll(): Flow<List<User>>
   }
   ```

4. **ContentProvider**：适合跨应用共享。  

5. **DataStore**：替代SharedPreferences的异步、协程支持存储（Proto/Preferences）。  
   ```kotlin
   val dataStore = context.createDataStore("settings")
   val nameFlow: Flow<String> = dataStore.data.map { it["name"] ?: "" }
   ```

---

### 6. MQTT 通信协议

**原理**：MQTT是一种基于发布/订阅模式的轻量级物联网协议，通过TCP长连接实现，最小报文仅2字节。有三个QoS等级（0最多一次，1至少一次，2只有一次）。通过Broker中转消息，客户端通过Topic订阅。适用于低带宽、不稳定的网络环境。

**Android中使用Eclipse Paho示例**  
```kotlin
// 添加依赖：org.eclipse.paho:org.eclipse.paho.client.mqttv3:1.2.5
// 由于Android限制，需使用MqttAndroidClient
val client = MqttAndroidClient(context, "tcp://broker.emqx.io:1883", clientId)
val options = MqttConnectOptions().apply {
    userName = "user"; password = "pass".toCharArray()
    isCleanSession = true
    keepAliveInterval = 60
}
client.connect(options, null, object : IMqttActionListener {
    override fun onSuccess(asyncActionToken: IMqttToken) {
        // 订阅主题
        client.subscribe("device/+/status", 1, null, object : IMqttActionListener { ... })
    }
    override fun onFailure(asyncActionToken: IMqttToken, exception: Throwable) { }
})

// 设置消息回调
client.setCallback(object : MqttCallback {
    override fun messageArrived(topic: String?, message: MqttMessage?) {
        val payload = String(message?.payload ?: byteArrayOf())
        // 处理消息，可配合EventBus或LiveData传递
    }
    // 连接断开自动重连等
})

// 发布消息
val msg = MqttMessage("payload".toByteArray()).apply { qos = 1 }
client.publish("topic/test", msg)
```

**设备通信场景**：APP订阅设备状态topic，发布控制指令topic；消息推送可用MQTT实现后台长连接推送（如使用Service保活，替换FCM）。

---

### 7. 安卓定位框架与第三方SDK

**Android原生定位**：基于LocationManager，通过GPS、网络、基站等Provider。  
```kotlin
val lm = getSystemService(Context.LOCATION_SERVICE) as LocationManager
if (checkSelfPermission(Manifest.permission.ACCESS_FINE_LOCATION) == PackageManager.PERMISSION_GRANTED) {
    lm.requestLocationUpdates(LocationManager.GPS_PROVIDER, 1000L, 1f, locationListener)
}
```

**高德/百度定位SDK**：封装了更精准的混合定位、地理围栏、轨迹纠偏等功能。  
示例（高德定位SDK）：  
```kotlin
// 初始化
AMapLocationClient.setApiKey("your_key")
val client = AMapLocationClient(context)
val option = AMapLocationClientOption().apply {
    locationMode = AMapLocationClientOption.AMapLocationMode.Hight_Accuracy
    isNeedAddress = true
    interval = 2000
}
client.setLocationOption(option)
client.setLocationListener { location ->
    if (location.errorCode == 0) {
        val lat = location.latitude
        val lng = location.longitude
        // 上报位置到服务器或MQTT
    }
}
client.startLocation()
```

**轨迹上报**：定时采集坐标，可在Service中使用Handler或协程周期上报，配合后台定位权限。轨迹绘制可在地图上用Polyline画出历史坐标。注意省电优化（降低频率、批量上报、使用被动定位）。

---

### 8. Gradle 项目构建

**原理**：Gradle是构建自动化工具，Android工程使用Android Gradle Plugin。核心概念：Project、Task、Plugin。编译流程：资源合并→编译器（AAPT2编译资源，javac→字节码，dx/d8→dex）→打包签名→对齐。

**多渠道打包**：在productFlavors中定义不同渠道，可替换占位符、资源文件。  
```groovy
android {
    flavorDimensions "channel"
    productFlavors {
        google { dimension "channel"
            buildConfigField "String", "CHANNEL", "\"google\""
            manifestPlaceholders = [CHANNEL_VALUE: "google"]
        }
        huawei { dimension "channel"
            buildConfigField "String", "CHANNEL", "\"huawei\""
        }
    }
}
// 利用buildConfigField生成BuildConfig常量
// 也可以使用walle等工具打包后写入渠道信息到APK签名区块
```

**依赖管理**：通过`implementation`（编译时依赖，不可传递）、`api`（可传递）、`compileOnly`等配置。统一版本管理可使用gradle.properties或buildSrc。  
```groovy
ext.versions = [kotlin: '1.8.0', okhttp: '4.10.0']
implementation "com.squareup.okhttp3:okhttp:$versions.okhttp"
```

**编译优化**  
- 开启Build Cache：`gradle.properties`中添加`android.enableBuildCache=true`  
- 增量编译：`org.gradle.parallel=true`, `org.gradle.caching=true`  
- 使用R8/ProGuard进行代码混淆和优化：`minifyEnabled true`, `proguardFiles ...`  
- 避免使用过时Transform，转向ASM字节码插桩。  
- 模块化，拆分library模块并行编译。

---

### 9. APP 性能优化

**内存排查**  
- 使用Android Profiler / MAT分析heap dump，查找泄漏对象和GC root引用链。  
- LeakCanary集成自动检测Activity/Fragment泄漏。  
常见泄漏：非静态内部类持有外部引用（Handler）、单例持有Context、资源未关闭（Cursor、Receiver未注销）。  
```kotlin
// 避免Handler泄漏
class MyHandler(activity: Activity) : Handler(Looper.getMainLooper()) {
    private val weakActivity = WeakReference(activity)
    override fun handleMessage(msg: Message) {
        weakActivity.get()?.let { // do something }
    }
}
```

**卡顿分析**  
- 主线程耗时操作导致帧丢失。使用StrictMode检测磁盘读写、网络操作。  
- BlockCanary / systrace / Perfetto定位耗时方法。  
- 布局优化：减少层级（ConstraintLayout）、ViewStub延迟加载、减少过度绘制（调试GPU过度绘制）。  
- RecyclerView优化：设置HasFixedSize，复用池，预加载，注意onBindViewHolder避免复杂计算。

**耗电优化**  
- 减少唤醒：JobScheduler / WorkManager合并任务。  
- 网络优化：合并请求、降低频率，使用低耗电模式（延迟不敏感用WorkManager的约束）。  
- 定位优化：按需停止定位，使用被动定位，降低GPS频率。  
- 后台限制：Android 8.0后后台服务限制，用JobIntentService或前台服务。

---

### 10. Android 权限适配与高版本系统适配

**权限适配**  
- Android 6.0+ 动态权限申请：危险权限需代码请求。  
```kotlin
ActivityCompat.requestPermissions(this, arrayOf(Manifest.permission.CAMERA), 100)
// 在onRequestPermissionsResult处理结果
```
- 使用第三方库（如PermissionX）简化。  
- 权限分组：同一组内一个权限通过，其他自动授予。  
- “不再提醒”处理：引导用户到设置页。  
- Android 11+分区存储（Scoped Storage），需要MediaStore API或使用文件管理器。对于私有目录（app-specific）可直接访问。  
- Android 13细化的媒体权限：READ_MEDIA_IMAGES/VIDEO/AUDIO。

**高版本系统适配**  
| 系统版本 | 适配点 | 代码示例 |
|----------|--------|----------|
| Android 8.0 | 通知渠道；后台服务限制；广播限制 | `NotificationChannel`创建，前台Service需startForeground |
| Android 9.0 | 明文HTTP限制（需配置network_security_config）；限制后台调用麦克风/相机 | `android:usesCleartextTraffic="true"` |
| Android 10 | 分区存储；后台启动Activity限制；深色主题 | `requestLegacyExternalStorage` (暂时兼容) |
| Android 11 | 强制执行分区存储；权限自动重置；软件包可见性 | `<queries>`声明要交互的应用 |
| Android 12 | SplashScreen API；待处理intent必须声明可变性；精确闹钟权限 | `android:exported`强制声明 |
| Android 13 | 通知运行时权限 `POST_NOTIFICATIONS`；照片选择器；WIFI权限独立 | `requestPermission(Manifest.permission.POST_NOTIFICATIONS)` |

**碎片化兼容**  
- 使用support库（AndroidX），版本兼容性由官方处理。  
- 多分辨率适配：使用dp/sp，布局使用ConstraintLayout，提供不同资源（drawable-xxxhdpi, values-sw600dp等）。  
- 厂商适配：针对华为/小米推送通道、后台允许权限引导。  
- 使用Build.VERSION.SDK_INT判断版本做分支处理。

---

以上每个主题均可深入展开，面试中通常结合具体场景考察实现细节、原理深度和实际解决问题能力。建议在准备时结合项目经验，思考常见陷阱和优化方案。