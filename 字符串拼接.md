
## 5种主要拼接方式

### 1. 使用 `+` 操作符

#### Java 示例

```java
String s1 = "Hello";
String s2 = "World";
String result = s1 + " " + s2;  // result = "Hello World"
```

#### Kotlin 示例

```kotlin
val s1 = "Hello"
val s2 = "World"
val result = s1 + " " + s2  // result = "Hello World"
```

#### 原理

- **单次拼接**：编译器优化为 `StringBuilder`
    
    ```java
    // 实际执行的字节码String result = new StringBuilder()    .append(s1)    .append(" ")    .append(s2)    .toString();
    ```
    
- **循环中拼接**：编译器无法优化，每次都创建新对象
    
    ```java
    String result = "";for (int i = 0; i < 1000; i++) {    result = result + i;  // ❌ 每次创建新 StringBuilder + 新 String}
    ```
    

#### 性能

- ✅ 单次拼接：快（编译器优化）
- ❌ 循环拼接：极慢（O(n²)复杂度）

---

### 2. 使用 StringBuilder（推荐）

#### Java 示例

```java
StringBuilder sb = new StringBuilder();
sb.append("Hello");
sb.append(" ");
sb.append("World");
String result = sb.toString();  // "Hello World"
```

#### Kotlin 示例

```kotlin
val sb = StringBuilder()
sb.append("Hello")
sb.append(" ")
sb.append("World")
val result = sb.toString()
```

#### 原理

- **容量管理**：初始容量通常是 16，超过容量时自动扩容
    
    ```java
    // 容量倍增策略（JDK实现）int newCapacity = (value.length << 1) + 2;  // 原容量 * 2 + 2
    ```
    
- **内存结构**
    
    ```
    StringBuilder 对象├── value[]  (字符数组，实际存储数据)├── count     (当前使用长度)└── capacity  (当前容量)append 操作：直接写入数组，不创建新对象
    ```
    

#### 性能

- ✅ 循环拼接：快（O(n)复杂度）
- ✅ 指定初始容量时最优

---

### 3. 使用 StringBuffer（线程安全）

#### Java 示例

```java
StringBuffer sb = new StringBuffer();
sb.append("Hello");
sb.append(" ");
sb.append("World");
String result = sb.toString();
```

#### 原理

- **完全同步版本**：所有方法都加 `synchronized` 关键字
    
    ```java
    public synchronized StringBuffer append(String str) {    toStringCache = null;    super.append(str);    return this;}
    ```
    
- **开销**：每次调用需要获取监视器锁

#### 性能

- ❌ 比 StringBuilder 慢 10-20%（同步开销）
- ✅ 线程安全环境必选

#### 使用场景

- 多线程共享的拼接操作
- 深度递归且可能被多线程访问的场景

---

### 4. 字符串模板（Kotlin & Java 15+）

#### Kotlin 示例

```kotlin
val name = "World"
val result = "Hello $name"  // 直接变量
val result2 = "Hello ${name.uppercase()}"  // 表达式
```

#### Java 示例（Preview Feature - JDK 21+）

```java
String name = "World";
String result = STR."Hello \{name}";  // 模板字符串
```

#### 原理（Kotlin）

- **编译时处理**：转换为 StringBuilder
    
    ```java
    // Kotlin 源代码val result = "Hello $name"// 编译后字节码（近似）String result = new StringBuilder()    .append("Hello ")    .append(name)    .toString();
    ```
    

#### 原理（Java 文本块）

```java
String html = """
    <html>
      <body>Hello $name</body>
    </html>
    """;
```

#### 性能

- ✅ 与 StringBuilder 性能相当
- ✅ 可读性最优

---

### 5. String.join() 和 String.format()

#### Java 示例

```java
// join - 适合集合场景
List<String> list = Arrays.asList("a", "b", "c");
String result = String.join(",", list);  // "a,b,c"

// format - 格式化拼接
String result = String.format("Hello %s, age %d", "Alice", 25);

// concat - 直接拼接，较少使用
String result = "Hello".concat(" ").concat("World");
```

#### 原理

**join()**

```java
public static String join(CharSequence delimiter,
                           Iterable<? extends CharSequence> elements) {
    Objects.requireNonNull(delimiter);
    Objects.requireNonNull(elements);
    StringJoiner sj = new StringJoiner(delimiter);
    for (CharSequence cs: elements) {
        sj.add(cs);
    }
    return sj.toString();
}
```

**format()**

```java
// 使用 Formatter 类，支持 printf 风格格式化
public static String format(String format, Object... args) {
    return new Formatter().format(format, args).toString();
}
```

#### 性能

- `join()`: ⭐⭐⭐⭐ 最优（专为集合设计）
- `format()`: ⭐⭐⭐ 中等（涉及格式解析）
- `concat()`: ⭐⭐ 不推荐（链式调用创建多个对象）

---

## 原理深度解析

### String 的不可变性

```java
// String 是 final 类，不可修改
public final class String implements Serializable, Comparable<String>, CharSequence {
    private final char[] value;  // final 数组
    private final int hash;      // 缓存的 hashCode
    // ...
}
```

#### 为什么 String 设计为不可变？

1. **字符串池优化**：相同的字符串引用同一对象，节省内存
    
    ```java
    String s1 = "Hello";
    String s2 = "Hello";
    s1 == s2;  // true（指向同一对象）
    ```
    
2. **线程安全**：无需同步即可在多线程间传递
    
3. **安全性**：防止恶意修改（如数据库密码）
    
4. **HashMap key 优化**：hashCode 可以缓存
    

### 内存分配过程

#### 单个 + 拼接的字节码分析

```java
String result = "a" + "b" + "c";
```

**编译期优化**（Javac 直接常量折叠）：

```java
String result = "abc";  // 编译时已确定
```

#### 变量 + 拼接的字节码分析

```java
String s1 = "a";
String s2 = "b";
String result = s1 + s2;
```

**编译后的伪代码**：

```java
String s1 = "a";
String s2 = "b";
StringBuilder sb = new StringBuilder();
sb.append(s1);
sb.append(s2);
String result = sb.toString();
```

**字节码指令序列**：

```
LDC     "a"
ASTORE_1         // s1 = "a"
LDC     "b"
ASTORE_2         // s2 = "b"
NEW StringBuilder
DUP
INVOKESPECIAL    // new StringBuilder()
ALOAD_1
INVOKEVIRTUAL    // append(s1)
ALOAD_2
INVOKEVIRTUAL    // append(s2)
INVOKEVIRTUAL    // toString()
ASTORE_3         // result
```

### StringBuilder 的扩容机制

```java
public AbstractStringBuilder append(String str) {
    if (str == null)
        return appendNull();
    int len = str.length();
    ensureCapacityInternal(count + len);  // 检查容量
    str.getChars(0, len, value, count);   // 复制字符
    count += len;
    return this;
}

private void ensureCapacityInternal(int minimumCapacity) {
    if (minimumCapacity - value.length > 0) {
        value = Arrays.copyOf(value, newCapacity(minimumCapacity));
    }
}

private int newCapacity(int minCapacity) {
    // 新容量 = 原容量 * 2 + 2
    int newCapacity = (value.length << 1) + 2;
    return (newCapacity - minCapacity > 0)
        ? newCapacity
        : minCapacity;
}
```

**扩容过程示意**：

```
初始: capacity = 16, count = 0

append("Hello")        // 5 chars
  → count = 5, capacity = 16

append(" ")           // 1 char
  → count = 6, capacity = 16

append("World")       // 5 chars
  → count = 11, capacity = 16

append("!!!") * 10    // 30 chars
  → count = 41, capacity = 34
    → 触发扩容: capacity = 34 * 2 + 2 = 70
    → 复制原数据到新数组（O(n)操作）
  → count = 41, capacity = 70
```

---

## 性能对比

### 1. 不同方式的速度对比

#### 测试场景：拼接 10000 个数字字符串

```java
public class StringConcatenationBenchmark {
    private static final int ITERATIONS = 10000;

    // 方式1: + 拼接（循环）❌ 最慢
    public static String plusOperator() {
        String result = "";
        for (int i = 0; i < ITERATIONS; i++) {
            result = result + i;
        }
        return result;
    }

    // 方式2: StringBuilder（推荐）✅ 最快
    public static String stringBuilder() {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < ITERATIONS; i++) {
            sb.append(i);
        }
        return sb.toString();
    }

    // 方式3: StringBuilder 预分配容量 ✅ 略快
    public static String stringBuilderWithCapacity() {
        StringBuilder sb = new StringBuilder(ITERATIONS * 5);
        for (int i = 0; i < ITERATIONS; i++) {
            sb.append(i);
        }
        return sb.toString();
    }

    // 方式4: String.format ⭐ 中等
    public static String stringFormat() {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < ITERATIONS; i++) {
            sb.append(String.valueOf(i));
        }
        return sb.toString();
    }

    // 基准测试
    public static void main(String[] args) {
        long start, end;
        
        // 测试 + 拼接
        start = System.nanoTime();
        plusOperator();
        end = System.nanoTime();
        System.out.println("+ 拼接: " + (end - start) / 1_000_000 + "ms");
        
        // 测试 StringBuilder
        start = System.nanoTime();
        stringBuilder();
        end = System.nanoTime();
        System.out.println("StringBuilder: " + (end - start) / 1_000_000 + "ms");
        
        // 测试预分配容量
        start = System.nanoTime();
        stringBuilderWithCapacity();
        end = System.nanoTime();
        System.out.println("StringBuilder(预分配): " + (end - start) / 1_000_000 + "ms");
    }
}
```

**测试结果（近似）**：

```
+ 拼接:              ~50000ms  ❌
StringBuilder:       ~2ms      ✅
StringBuilder(预分配): ~1ms     ✅✅
String.format:       ~8ms      ⭐
```

**时间复杂度分析**：

|方式|最好情况|平均情况|最坏情况|原因|
|---|---|---|---|---|
|`+` 循环|O(n²)|O(n²)|O(n²)|每次创建新对象|
|StringBuilder|O(n)|O(n)|O(n log n)|扩容时复制|
|StringBuilder(预分配)|O(n)|O(n)|O(n)|无扩容|
|StringBuffer|O(n)|O(n)|O(n)|同步开销 + 扩容|
|String.join|O(n)|O(n)|O(n)|优化的集合遍历|

### 2. 内存使用对比

#### + 拼接（n=10）时的内存分配

```
迭代1: "" + "1"     → 创建 1 个临时 String（2字符）
迭代2: "1" + "2"    → 创建 1 个临时 String（3字符）
迭代3: "12" + "3"   → 创建 1 个临时 String（4字符）
...
迭代10: "123456789" + "10" → 创建 1 个临时 String（11字符）

总创建对象: 10 个
总字符数: 1 + 2 + 3 + ... + 11 = 66 个

内存浪费: 66 - 10 = 56 个字符（相当于 112 字节）
```

#### StringBuilder 方案

```
只创建 1 个 StringBuilder 对象
初始容量: 16
一次扩容: 16 * 2 + 2 = 34
二次扩容: 34 * 2 + 2 = 70

最终使用: 10 个字符
浪费: 70 - 10 = 60 个字符（但可复用）
```

---

## 最佳实践

### 1. 选择标准

```
┌─ 什么场景？
│
├─ 单次拼接（2-3 个字符串）
│  └─ 用 + 或字符串模板 ✅
│     String result = s1 + " " + s2;
│     String result = "${s1} ${s2}";  // Kotlin
│
├─ 多个字符串拼接
│  └─ 用 StringBuilder ✅
│     StringBuilder sb = new StringBuilder();
│     for (String s : list) sb.append(s);
│
├─ 集合拼接（指定分隔符）
│  └─ 用 String.join() ✅
│     String result = String.join(",", list);
│
├─ 格式化字符串
│  └─ 用字符串模板或 format ✅
│     String.format("Name: %s, Age: %d", name, age)
│
└─ 多线程环境
   └─ 用 StringBuffer ✅
      StringBuffer sb = new StringBuffer();
```

### 2. 代码示例

#### ❌ 错误做法

```java
// 1. 循环中使用 +
String result = "";
for (String item : items) {
    result = result + item;  // 极慢！
}

// 2. 链式 concat
String result = "a".concat("b").concat("c");  // 创建 2 个临时对象

// 3. 未指定容量的 StringBuilder
StringBuilder sb = new StringBuilder();
for (int i = 0; i < 100000; i++) {
    sb.append("a".repeat(100));  // 多次扩容
}
```

#### ✅ 正确做法

```java
// 1. 使用 StringBuilder
StringBuilder sb = new StringBuilder();
for (String item : items) {
    sb.append(item);
}
String result = sb.toString();

// 2. 预估容量
int estimatedSize = items.stream().mapToInt(String::length).sum();
StringBuilder sb = new StringBuilder(estimatedSize);
for (String item : items) {
    sb.append(item);
}

// 3. 使用 String.join
String result = String.join("", items);

// 4. Kotlin 字符串模板
val result = "$name is $age years old"

// 5. 一次性拼接多个常量
String result = s1 + " " + s2 + " " + s3;  // 编译器优化为单个 StringBuilder
```

### 3. Kotlin 特殊优化

#### Kotlin buildString（最佳实践）

```kotlin
val result = buildString {
    append("Hello")
    append(" ")
    append("World")
    append("!")
}

// 等价于
val result = StringBuilder().apply {
    append("Hello")
    append(" ")
    append("World")
    append("!")
}.toString()
```

#### Kotlin 字符串模板（自动优化）

```kotlin
val name = "Alice"
val age = 25
val result = "Name: $name, Age: $age"  // 编译器自动优化

// 嵌套表达式
val result = "Sum: ${a + b}, Product: ${a * b}"
```

---

## 总结表

|特性|+ 操作符|StringBuilder|StringBuffer|String.join|字符串模板|
|---|---|---|---|---|---|
|单次拼接|✅✅|✅|✅|❌|✅✅|
|循环拼接|❌❌|✅✅|✅|✅✅|✅✅|
|集合拼接|❌|✅|✅|✅✅|✅|
|线程安全|✅|❌|✅|✅|✅|
|可读性|✅✅|✅|✅|✅✅|✅✅|
|性能|❌❌(循环)|✅✅|✅|✅✅|✅✅|
|内存效率|❌❌(循环)|✅|✅|✅|✅|

**核心原则**：

- 🎯 **单次拼接**：用 `+` 或字符串模板，编译器会优化
- 🎯 **循环/多次拼接**：用 `StringBuilder`，记得预分配容量
- 🎯 **集合拼接**：用 `String.join()`，最简洁高效
- 🎯 **多线程**：用 `StringBuffer`，虽然有同步开销