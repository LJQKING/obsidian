
## 一、基本数据类型（Primitive Types）

|类型|大小|默认值|包装类|Android 使用场景|
|---|---|---|---|---|
|`byte`|1B|0|Byte|二进制数据、音频帧|
|`short`|2B|0|Short|极少用，节省内存时|
|`int`|4B|0|Integer|计数、ID、View尺寸|
|`long`|8B|0L|Long|时间戳、文件大小|
|`float`|4B|0.0f|Float|动画插值、传感器数据|
|`double`|8B|0.0|Double|高精度计算、GPS坐标|
|`char`|2B|'\u0000'|Character|字符处理（UTF-16）|
|`boolean`|1B|false|Boolean|状态标志|

- [[A 网络通信｜应用架构｜handler和线程池.md]]  Handler消息机制
- [[Android 开发高级岗位.md]]  车载智能座舱
- [[线程通信 + 进程通信 + BinderAIDL + Java并发.md]]  Handler消息机制
- [[车载智能座舱Android工程师.md]]  车载智能座舱
- [[5.1 Handler 源码级分析及原理.md]]  Handler消息机制
**核心原理**：基本类型存在**栈帧的局部变量表**中，不经过 GC，访问速度远快于引用类型。Android 中 `int[]` 比 `Integer[]` 少约 5 倍内存，因为包装类有对象头（16B）+ 字段（4B）= 20B，而 `int` 只有 4B。

---

## 二、引用数据类型（Reference Types）

### String — 深度解析

```java
// 字符串常量池（String Pool）机制
String a = "hello";          // 存入常量池（堆中的特殊区域）
String b = "hello";          // 直接复用，a == b → true
String c = new String("hello"); // 强制堆分配，a == c → false

// StringBuilder：非线程安全，性能最好，Android 循环拼接必用
StringBuilder sb = new StringBuilder();
for (int i = 0; i < 100; i++) sb.append(i);

// StringBuffer：线程安全（synchronized），Android 多线程场景
```

**内部原理**：Java 9+ String 底层从 `char[]` 改为 `byte[]` + encoding flag，拉丁字符节省一半内存，Android API 34+ 的 ART 虚拟机已采用此优化。

### 包装类与自动装箱陷阱

```java
// 自动拆装箱（Autoboxing）—— 智能座舱高频性能坑
Integer x = 127;  // 缓存范围 [-128, 127]，从缓存取
Integer y = 127;
x == y; // true（同一对象）

Integer m = 128;
Integer n = 128;
m == n; // false（两个不同堆对象！）

// Android 中避免在频繁调用的回调里做装箱
// 错误：Map<Integer, Object> → 正确：SparseArray<Object>
```

---

## 三、集合框架（Collections Framework）

### 3.1 List 系列

#### ArrayList — 底层原理

```java
// 底层：Object[] elementData，初始容量 10
// 扩容：newCapacity = oldCapacity + (oldCapacity >> 1) = 1.5倍

public boolean add(E e) {
    ensureCapacityInternal(size + 1); // 检查是否需要扩容
    elementData[size++] = e;          // 直接赋值 O(1)
    return true;
}

// 扩容时调用 Arrays.copyOf → System.arraycopy（native）
// 中间插入/删除：O(n)，需要移动后续元素

// Android 使用建议
ArrayList<String> list = new ArrayList<>(50); // 预估容量，避免多次扩容
```

**车机场景**：RecyclerView 的数据源、carPropertyValues 列表缓存用 ArrayList，预知数量时传入初始容量。

#### LinkedList — 双向链表

```java
// Node<E>：prev + item + next，每个节点多 24B 对象头开销
// 插入删除 O(1)（已有引用时），随机访问 O(n)
// Android 中常用作 Deque（双端队列），实现 BFS 广度优先搜索
LinkedList<Integer> deque = new LinkedList<>();
deque.addFirst(1); deque.addLast(2); deque.pollFirst();
```

---

### 3.2 Map 系列 — 面试核心

#### HashMap 完整原理（Java 8+）

```
内部结构：
Node<K,V>[] table  ← 哈希桶数组（默认长度16，负载因子0.75）
     │
     ├── table[0]: null
     ├── table[1]: Node → Node → Node  ← 链表（<8个）
     ├── table[5]: TreeNode ← 红黑树（≥8个 且 table.length≥64）
     └── ...
```

```java
// put 完整流程
map.put(key, value);
// 1. key.hashCode() → 扰动函数 h^(h>>>16) → 低位更散列
// 2. index = (n-1) & hash（等价于 hash % n，但更快）
// 3. table[index] 为空 → 直接插入
// 4. 不为空 → equals 比较 key → 相同则覆盖，不同则链表追加
// 5. 链表长度 ≥ 8 且 table.length ≥ 64 → 树化（treeifyBin）
// 6. size > threshold(capacity*0.75) → resize() 扩容为 2 倍

// 扩容关键：2的幂次保证 (n-1)&hash 均匀分布
// 树化阈值 = 8 是泊松分布下碰撞概率 < 千万分之一的设计
```

#### Android 专属 Map 对比

```java
// SparseArray：int key，无装箱，二分查找（适合 < 1000 条）
SparseArray<String> sparse = new SparseArray<>();
sparse.put(100, "音量");   // key=int，无 Integer 装箱开销

// ArrayMap：任意 key，双数组存储（hashes[] + Object[] pairs）
// 内存比 HashMap 少 ~50%，适合小数据量（< 1000）
ArrayMap<String, String> arrayMap = new ArrayMap<>();

// HashMap：大数据量（> 1000），需要高并发时用 ConcurrentHashMap
// 车机中 VHAL 属性缓存、配置项映射推荐 ArrayMap
```

---

### 3.3 Queue / Deque 体系

```java
// PriorityQueue：小根堆（完全二叉树），O(log n) 入队出队
// 车机场景：按优先级处理 HMI 事件队列
PriorityQueue<Task> pq = new PriorityQueue<>((a, b) -> a.priority - b.priority);

// ArrayDeque：双端队列，底层循环数组，比 LinkedList 快 ~2x
// 替代 Stack（Stack 是遗留类，synchronized，性能差）
ArrayDeque<String> stack = new ArrayDeque<>();
stack.push("frame1"); stack.pop(); // 当栈用

// BlockingQueue：生产者-消费者模式，Handler 底层 MessageQueue 的思路
LinkedBlockingQueue<Runnable> queue = new LinkedBlockingQueue<>(128);
```

---

## 四、线程安全集合

|集合|机制|适用场景|
|---|---|---|
|`Vector`|synchronized 全锁|已过时，不推荐|
|`CopyOnWriteArrayList`|写时复制新数组|读多写少的监听器列表|
|`ConcurrentHashMap`|CAS + synchronized 分段|高并发 K-V 缓存|
|`Collections.synchronizedList()`|包装器，全锁|简单场景|

```java
// Android 多线程典型：监听器列表用 CopyOnWriteArrayList
// 避免 forEach 时 add/remove 导致 ConcurrentModificationException
private final CopyOnWriteArrayList<Listener> listeners = new CopyOnWriteArrayList<>();

// 车机 VHAL 回调缓存：
private final ConcurrentHashMap<Integer, CarPropertyValue<?>> propCache 
    = new ConcurrentHashMap<>();
```

---

## 五、Bundle — Android 专属

```java
// Bundle 底层：ArrayMap<String, Object>
// 支持 Parcelable（Binder 传输），不支持 Serializable（慢 10x）
Bundle bundle = new Bundle();
bundle.putInt("speed", 80);
bundle.putParcelable("location", location); // 实现 Parcelable
// 跨进程：Binder → 序列化为 Parcel → 反序列化

// 注意：Bundle 有 1MB 传输限制（Binder 缓冲区），大数据用文件/共享内存
```

---

## 六、车机/AAOS 场景选型速查

|场景|推荐结构|原因|
|---|---|---|
|VehicleProperty 缓存|`SparseArray`|key 是 int，无装箱|
|HMI 配置项|`ArrayMap`|少量 K-V，低内存|
|监听器列表|`CopyOnWriteArrayList`|读多写少，无锁读|
|消息队列|`ArrayDeque`|Handler 任务调度|
|大数据集排序|`ArrayList` + `Collections.sort`|Timsort O(n log n)|
|并发属性读写|`ConcurrentHashMap`|多线程安全|
|组件通信参数|`Bundle` + `Parcelable`|Binder IPC 标准方式|

核心口诀：**小数据用 Array 系，并发用 Concurrent 系，车机 int key 用 Sparse 系，跨进程用 Bundle+Parcelable。**